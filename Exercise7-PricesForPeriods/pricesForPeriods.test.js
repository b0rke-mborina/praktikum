const pricesForPeriods = require('./pricesForPeriods');

describe('log to console prices for given periods from lowest to highest', () => {
	it("call function:", () => {
		pricesForPeriods();
	});
});